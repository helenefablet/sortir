{% extends 'base.html.twig' %}

{% block title %}Modifier Sortie{% endblock %}

{% block body %}

    <h1>Modifier Sortie</h1>

    {{ form_start(form) }}

        {{ form_row(form.nom) }}
        {{ form_row(form.dateHeureDebut) }}
        {{ form_row(form.dateLimiteInscription) }}
        {{ form_row(form.nbInscriptionsMax) }}
        {{ form_row(form.duree) }}
        {{ form_row(form.infosSortie) }}
        {#{{ form_row(form.ville) }}#}
        {#{{ form_row(form.lieu) }}#}


        <button class="btn">{{ button_label|default('Modifier') }}</button>

    {{ form_end(form) }}

    <a href="{{ path('accueil') }}">Retour à la liste</a>
    <a href="{{ path('sortie_delete', {'id': sortie.id}) }}">Supprimer</a>

{% endblock %}
