{% extends 'base.html.twig' %}

{% block title %}Nouveau campus{% endblock %}

{% block body %}
    <h1>Nouveau campus</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-info">{{ button_label|default('Enregistrer') }}</button>
    {{ form_end(form) }}


    <a href="{{ path('campus_index') }}" class="btn btn-outline-info">Retour à la liste</a>
{% endblock %}
